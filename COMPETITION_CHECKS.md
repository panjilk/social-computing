# 渠道竞争模型：机制解释与局部可靠性检查

2026-09-17。研究范围仍是国内正式渠道与商业客户渠道的竞争，不加入制度改善公告或政策冲击。

## 这次增加什么

1. 同一个初始案件在“仅国内/仅客户”与“双渠道共存”之间的实际路径配对。六类路径互斥：未提交、仅国内、仅客户、首次同时、国内后客户、客户后国内。每张转移矩阵的全部单元格之和为100%，不是逐行条件比例。
2. 关闭追加、关闭同事影响、同时关闭两机制，与基准形成2×2机制组合。关闭追加仍允许首次同时使用；关闭同事影响同时将行动成本影响和成功/失败观察更新置零。
3. 对称诊断：匹配两渠道可达性、成本/主观预期的分布、每个处理阶段的成功概率、等待时间，并消除容量限制；关闭同事影响。用于排查偏向，不用于模拟现实政策变化。
4. 局部稳健性：WS/BA网络；人口与容量同比加倍；观察期加倍；两渠道成本均提高20%；先验均降低0.10；预计等待均加倍。没有将这些作为新的主研究问题。

每个实验块仍运行仅国内、仅客户、共存三组。默认12块×3组×20连续种子，共720次。小规模检查12块×3组×2种子，共72次。使用种子0–19而非按结果选择；此前90次主实验用了0–29，所以新基准均值不必与旧报告完全相同。

## 配对和统计

各块内三组的网络、初始争议、归属、个体差异和按轮/员工索引的潜在随机流保持相同。三个机制关闭对照沿用基准同一世界。不同人口规模无法逐人配对，仅比较各块内部渠道共存效应。所有配对案例用 case_id、employee、firm、created 联合键合并并检查无缺失，不能按文件行号合并。

先对每个种子计算比例，再等权平均，不将所有案例合并后假装相互独立。均值差的95%置信区间使用配对种子bootstrap百分位法，默认2000次重采样，统计随机流与模型随机流独立。不把SD称为置信区间，也不将未排除零当成证实无效。多个指标的区间未经多重比较修正，用于探索。

关闭追加或同事影响的效果以“关闭组减基准共存组”定义。两因素交互以 基准−关闭追加−关闭同事影响+同时关闭 计算。交互不为零时尤其不能把各机制作用机械相加。

## 使用量拆分的边界

在同一条渠道上，净使用变化 = 单渠道未用而共存使用 − 单渠道使用而共存未用。后一类再按共存时已解决/未解决划分，严格核对数目守恒。

这是案件路径的描述性核对，不是中介效应的因果分解。“未使用客户但国内已解决”不代表已经证明国内解决导致其放弃客户；改变可用渠道会一起改变选择、时间和同事经历。“未解决且没用客户”也不能全部归于成本太高。需要保留这种限制。

## 对称诊断具体含义

国内第一阶段为 q×(0.5+0.5m)，客户审核设为 q/2+(q/2)m；国内落实成功率与客户介入后的企业成功率相等，不受依赖参数影响。所有可用性设为1，两个阶段的延迟匹配，各容量设到全体人数以避免拥堵。个体两渠道成本/信念是独立同分布抽样，事件仍独立，不要求两条曲线逐个种子相同。

额外逐员工交换成本和信念，检查其第一次选择是否相应镜像。还构造精确收益相等的例子：现有候选顺序是不追加、国内、客户、同时。若只有国内与客户同为最高，默认偏国内；该行为明确测试和记录，本次没有悄悄修改。以后若采用离散同质参数，需明确考虑这个平局规则。

## 文件与命令

```powershell
.\run.ps1 -m unittest discover -s tests -q
.\run.ps1 -m experiments.competition_checks --small --repeats 2 --out outputs/competition_checks_small
.\run.ps1 -m experiments.competition_checks --out outputs/competition_checks_v1
```

配置 configs/competition_checks.json；基准配置 configs/competition.json。输出包括完整案件、逐轮数据、六分类配对路径、使用增减拆分、配对差置信区间、三个中文图、运行配置与源码哈希、验证记录、中文结果报告。种子0的主要机制组额外保存决策/事件日志。

## 尚未完成的论文工作

没有现实参数估计；没有系统检索证明创新性；没有验证员工实际采用当前收益规则；没有全局敏感性分析；没有机构主动调整策略的博弈。增加这些检查提升模型内解释的可信度，但不能替代现实验证或保证发表。若结果表明社会网络作用小，应缩减相应论文主张，不人为增加网络强度以制造预期结果。
