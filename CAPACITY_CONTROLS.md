# 客户容量分配与国内拥堵：新增对照

本实验回应“为什么国内不会拥堵”：国内确实有容量限制。旧客户网络实验始终固定国内机构容量12，没有单独扫描。本实验分别控制客户资源分配与国内机构容量，观察既有双渠道共存模型中的首次选择、后续追加和结果。

## 设计

全部组均为双渠道共存，使用相同初始案件；不是三种渠道可用性主实验的替代。客户数6、企业6、120员工、45轮；企业均有主要客户、员工可达概率1。保持客户总容量12、企业依赖及其分布、成本、客观成功规则、主观先验和预计等待固定。新争议、公告、材料帮助、预防均关闭。参数仅为探索值。

3种客户容量规则×3种连接结构×3种国内机构容量×20配对种子=540次；先运行2种子的54次小规模验证。

|客户规则|6个客户连接企业|3个客户连接企业|1个客户连接企业|
|---|---|---|---|
|equal：均分|各2名额|各2名额，其余6名额闲置|2名额，其余10名额闲置|
|degree：按连接企业数分配|各2名额|各4名额|12名额|
|pooled：全局共享|统一FIFO，12名额|统一FIFO，12名额|统一FIFO，12名额|

degree规则：客户j的连接企业数为d_j，原始配额 q_j=12 d_j/Σd_j，先向下取整，再按余数大小分配剩余整数名额（余数相同按客户编号）。本实验配额都为整数。该规则在模拟开始时固定，不按当轮案件量自动调整；无连接时均分保留资源但无案件可访问。pooled与degree在一般连接结构下不同：degree的空闲名额不跨客户转移。

国内只改变机构处理容量：2、6、12；国内落实容量保持12，成功率和延迟不变。因此可以把组间差异归于本模型中机构容量设定的改变，但不能将所有行为变化都归因于等待这一条中介路径。不是时间上的政策改善实验。

## 等口径的拥堵指标

双方均记录**轮末、已经达到最短处理等待、尚未响应/审核且未解决的案件**。某案提交s、最短延迟δ、响应r、解决z，则在轮t计入积压当且仅当 s+δ≤t<min(r,z)。缺失终点用观测期末之后替代。已被另一渠道解决的案件退出积压，失败结束不再在队列中。

逐轮积压求和为案件×轮；它不是独立案件人数。国内字段d_eligible，客户字段e_eligible；端点d_eligible_rounds、e_eligible_rounds。两者只统计首个机构/客户审核阶段，不包括支持后的落实和客户介入后的企业队列。

旧customer_network_v1中的客户eligible_queue_rounds在客户审核选择后、当轮解决前记录，口径与本次轮末指标不同，不可直接拼表比较。

使用、解决、双向追加比例分母均为全部初始争议案件；当前每人只有一个案件。累计国内和客户使用允许重叠。当前预计等待仍固定；员工不预先知道国内容量或客户拓扑变化，因此首次决策不应被解释为容量预告的反应。

## 配对与核验

种子0..19完整运行；员工、初始案件、社交网络、企业依赖由既有分离随机流生成。客户分配用独立流[seed,701]。事件按员工和轮次的潜在抽签配对，排队改变时可能使用不同轮次抽签。逐运行检查案件守恒、唯一结案/补偿、名额总量和处理上限。

边界核验：degree在6连接客户时与equal轨迹完全一致；degree在1连接客户时与pooled一致；pooled在所有连接结构下轨迹一致。单元测试覆盖非整除配额、无连接、取消退出队列、等待口径及可复现。

统计按独立种子配对作差，bootstrap2000次生成逐项95%区间，没有多重比较调整；图展示均值±1标准差。报告集中减分散、国内容量2/6减12、degree减equal以及两种容量规则的集中效应之差。所有对照都保存在paired_intervals.csv，不按显著性筛选结果。

## 运行与结果

```powershell
.\run.ps1 -m unittest discover -s tests -q
.\run.ps1 -m experiments.customer_capacity_controls --repeats 2 --out outputs/customer_capacity_controls_small
.\run.ps1 -m experiments.customer_capacity_controls
.\run.ps1 -m experiments.capacity_readout
```

配置configs/customer_capacity_controls.json；输出outputs/customer_capacity_controls_v1。包括案件、轨迹、配额、端点、配对区间、中文图、RESULTS.md、种子0事件决策和verification.json及tests.txt。不覆盖原网络实验或论文初稿。

capacity_readout只读取完整实验结果，补充choice_comparison.png；核验各组首次选择比例相同，以及已有customer_network_v1对应组端点完全一致（若旧结果存在）。它默认针对20种子正式实验目录；小规模运行不调用该附加脚本。

本实验尚未改变国内落实容量，不研究完整供应链或客户间动态转单。有限初始队列的平台不代表长期稳态；尚无现实校准，资源数字相同也不代表两个现实机构的处理成本可比。
