# 图片左图：使用替代与材料互补的第一轮研究

日期：2026-09-19。此实验检验图片提出的问题，不将图片曲线当作目标或经验数据。

## 问题与规则

国内更有效是否减少客户使用？客户使用者的成功比例是否提高？这种变化是选择者构成改变，还是材料作用？
只使用现有探索模型，不添加现实校准声明或未经核实的法律规则。

新增 `material_requires_support` 开关，旧配置默认false兼容旧行为；本实验true。
国内支持救济后，以 `material_rate*(0.5+0.5*merit)` 的概率形成支持性材料。
材料是模型中可供后续核查的支持性信息，不代表任意行政文书，也不是补救落实。
当轮生成材料不能当轮用于员工决策或客户审核，仍采用既有同步信息规则。
已经在客户审核后产生的材料不追溯改变审核结果；取消案件不能当成客户失败。

客户审核概率 `clip(review_base+review_merit*merit+evidence_help*material,0,1)`。
主观材料作用只在已获知材料后加到个人客户成功预期，二者分开开关。
两个概率增益均为假设，不是现实证据。即便模型中材料增益非负，总体使用或解决效果也未必单调。

## 预定设计

18组×连续20种子。每次240人、8批争议、每批观察24轮，沿用动态版本的配对与客户网络。

- 支持救济概率参数0.2/0.55/0.9；延迟固定2轮，落实概率0.85。
- 初次响应延迟0/2/6轮；支持概率固定0.55，落实概率0.85。
- 每种扫描分别对照：材料无作用、仅客观作用、客观+主观作用。
- 三模式均可能形成材料，区别在作用；避免将材料存在本身和增益混为一种干预。
- 材料生成率参数0.7，客观加成0.3，主观加成0.18。所有参数未经校准。
- 两个扫描共享的中心配置重复运行作为一致性检查，不视为独立额外证据。

只改变指定客观参数，不直接同步修改初始主观信念。各批后续员工通过可见处理经历学习。
本轮不同时改变赔偿收益、速度和落实概率；不加入企业预防。

## 分母与选择问题

后期=后两批合并，前期=前两批合并，种子内按案件数统计，跨种子等权。
国内与客户使用以全部争议案件为分母，允许重叠；另报两侧均可达人群。

客户路径成功比例=H内通过客户路径成功的案件/在H内使用客户的案件。
包含联合成功，但一案在该指标仅计一次；另报客户单独成功、联合成功。
使用客户的案件若后来被国内解决，计入“客户使用者任一渠道解决”，不因此计入客户路径成功。
仍未解决案件不从分母删除，另报未解决比例。

条件成功率受选择影响，不直接用它认定材料互补。增加：

1. **无材料标准得分**：对实际客户使用者，按相同无材料公式计算审核概率×企业后续落实概率的均值。
   参数扫描下此公式固定，变化反映被选择案件的属性构成，不是实际成功率。
2. **机械材料概率增量**：冻结实际客户使用者和审核时材料，计算有材料公式减无材料公式的路径概率差。
   它忽略国内提前解决、排队和取消，不是实际成功率的因果贡献。
3. **机制总影响**：相同种子材料开关的配对差，包含行为与信息反馈，不是固定使用者的直接效应。

三者不能相加成“三成筛选、七成互补”。如果需要正式因果分解，必须进一步定义反事实总体与中介估计对象。

## 运行与交付

```powershell
.\run.ps1 -m unittest discover -s tests -q
.\run.ps1 -m experiments.substitution_complementarity --repeats 2 --out outputs/substitution_complementarity_small
.\run.ps1 -m experiments.substitution_complementarity
.\run.ps1 -m experiments.substitution_readout
```

配置 `configs/substitution_complementarity.json`。主输出 `outputs/substitution_complementarity_v1/`：

- `RESULTS.md`：完整结果与解释限制。
- `metrics.csv`：种子、情景、前后期、人群的指标。
- `intervals.csv`：均值和逐项bootstrap95%区间。
- `paired_contrasts.csv`：极值间及材料模式之间的配对差；未作多重比较校正。
- `support_comparison.png`、`delay_comparison.png`：共同中文图例的四面板图；阴影为均值的逐项95%区间。
- `demo/`：种子0全部情景的案件、事件、决策记录。
- `verification.json`：配置、随机种子及源码哈希。
- `FINDINGS.md`、`additional_checks.json`、`material_timing_seed0.csv`：运行readout生成的通俗解读、中心配置一致性核查和示例材料时序。

本轮属于图片主线第一部分。未完成中图的企业预防/合规差距，也未完成右图持续争议下传播与预防反馈。
