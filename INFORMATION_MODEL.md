# 国内改善、信息获知与员工决策：实验说明

**2026-09-16 修订：基础模型为 v3，见 [DOMESTIC_PROCESS.md](DOMESTIC_PROCESS.md)。** 现有信息传播机制保留，但实际改善改为影响国内机构支持补救系数；独立落实阶段参数保持不变。新版输出 outputs/information_experiment_v2，旧目录结果不代表当前源码。

这是渠道 v2 的可选信息实验，不改变不传入该模块时的运行行为。对应 `src/information.py`、`experiments/information_experiments.py`，配置 `configs/information_experiment.json`。所有参数为探索值，未经校准。基础规则见 CHANNEL_MODEL.md。

## 研究目的

区分国内实际效果改善、员工接收到改善公告、观察邻居处理结果及最终投诉行为。既有主观信号扫描是不同初始环境的比较，本实验在同一运行的指定轮次改变条件。

## 处理设计

主实验 120 人、6 企业、50 轮。初始争议率 .3，无未解决案件员工每轮新争议概率 .03，预防关闭。第 10 轮实际国内效果参数从 .35 提高到 .75，实际延迟、容量、材料规则和客户依赖均保持不变。这是模型参数变化，不对应具体政策或日期。

四组：

|组别|实际效果|公告|
|---|---|---|
|unchanged|一直 .35|无|
|no_notice|第 10 轮起 .75|不主动告知，但允许邻居结果更新|
|immediate|第 10 轮起 .75|同轮决策前全体收到|
|network_notice|第 10 轮起 .75|约 10% 随机员工先收到，之后沿社交网络传播|

network_notice 是公告传播，不是成功经历传播。no_notice 的判断仍可能因邻居已知结果改变。个人经历通过当前案件等待、失败和材料影响决策，没有新增跨案件累积个人学习。

辅助实验块：learning_off（所有组关闭社会结果更新）、closed_cohort（关闭新争议）、network_ws 和 network_ba。共 5 块×4 组，正式运行每组 20 个连续种子。所有同块同种子在改变前的轨迹与首次选择必须相同。

## 公告传播规则

初始接收者独立 Bernoulli(seed_fraction=.1)，不强制至少一人，也不按中心性选人。第一个接收轮只初始化；之后每轮，未接收员工若有 m 个上一轮已获知且允许共享的邻居，则获知概率为 1-(1-transmission)^m，默认 transmission=.25。

只用上一轮的接收集合，不能同轮多跳。采用渠道模型既有的有向共享关系；隔离节点不能通过邻居获知，零种子时消息不会凭空产生。消息到达后持续记住，不失真。

公告可信程度沿用 domestic_information=.7，没有单独估计真实性、信任或注意力。每轮公告调整基准 p_base=clip(p_initial_clipped + informed*.7*(announced_signal-domestic_signal),0,1)。默认信号从 .35 变为 .75。该增量只施加一次，不逐轮累加；在基准发生截断时，不等同于重新生成一个不同初始世界。

随后使用有限窗口结果更新，得到通用主观概率。因此“收到公告”不等于知道实际全路径成功率；.75 是机构支持补救概率的一个系数，最终解决还受案件条件、独立落实概率、两阶段队列等影响。不能用 p 减 .75 直接宣称认知误差。

## 轮次顺序与客观效果

先生成新争议、冻结过去结果；公告到达并更新基准；记录全员判断；员工选择；机构处理、客户审核、国内落实与企业商业响应分别执行。改善轮起，仅尚待国内机构处理的请求使用新机构参数，已经支持但等待落实的案件不重新判断；不重开已结束或已解决案件。材料、落实概率和延迟不随改善改变。同轮刚处理结果下一轮才可观察。

信息随机源 SeedSequence([seed,200,key]) 与既有过程分离。信息数组按轮×员工生成，扩大期限保留原前缀。不同选择/结案使后续案件或处理时点变化，不能认为所有后续案件都逐一具有相同潜在结果。

## 记录与分母

- belief_panel：每轮决策前固定全体员工（包括没有争议者）的 prior_d、p_d、p_e、是否收到公告、可见成功/失败及当前案件。p_d/p_e 是通用判断，不含个案材料和等待折现；实际选择使用的概率与效用见 decisions.csv。
- informed：只表示收到改善公告，不代表未收到者不了解任何结果。
- t0/pre/post_first_e：相应时期首次仅 E 案件 / 相应时期首次投诉案件；D+E 单列。第 10 轮归 post。后期首次投诉人数可以变化，不能把条件比例差直接当成同一人群的偏好变化。
- pre_domestic_later_e：改善前首次仅 D 的同一批案件中，改善后追加 E 的比例。包含改善前已经解决者，分母在同块四组一致，不是仍在办者的追加风险。
- customer_share_all_cases：使用过 E 案件 / 所有发生案件。新争议只在没有未解决案件者中产生，因此结案改变再次发生争议的机会，案件分母会变化；closed_cohort 提供对照。
- post_mean_belief_d：改善后各轮全体员工判断的均值。固定员工分母用于排除仍在投诉者构成变化；不是案件成功率。

主实验 seed=0 四组完整面板保存在 demo，各组所有种子的逐轮全员均值、案件、决策、端点保存到批量 CSV。完整面板可用配置/种子重新运行得到。

统计先逐种子再汇总。图为均值 ±1 样本 SD，不是 CI。paired_differences.csv 提供配对差、SD、SE 和有效 n。SE 不等于 CI。没有用案件数作为独立实验重复。

## 图表解释

information_dynamics.png：左上收到公告比例，右上全体员工对国内渠道的平均判断，左下每轮客户提交数（含追加），右下未解决案件存量。虚线为改善轮。公告曲线的预设差异是实现检查，不是创新结论。

information_choices.png：改善后首次仅 E 比例、固定改善前国内首选队列的后期追加比例、最终未解决比例；图标题提醒条件分母不同。

## 验证与结论边界

测试无信息模块/无处理钩子对原模型的等价性、改善前组间一致、无公告且无结果更新时判断不泄漏实际改善、公告单次增量、手工网络一轮一跳、孤立/零共享、随机前缀与复现、无争议者仍有判断记录及案件守恒。

立即告知后判断提高是规则预设，不单独作为研究发现。重点观察不同信息机制是否影响实际选择、追加和争议负担，以及差异在关闭学习或新争议后如何变化。没有预设信息一定改善结果。

即使关闭结果更新且不公告，实际改善仍可提前解决案件、改变后续选择机会与邻居行动成本。因此“外部使用减少”本身不是判断改变的证据；须结合固定全员判断面板解释。初始 .35/.75、信号响应权重与传播参数均未校准，不能将各组间差异大小直接映射到现实。

未完成：公告与现实制度信息的对应依据、个人累计学习、信息失真、速度改善的平行实验、材料×企业依赖的完整交叉研究及创新性查新。不能将本实验称为现实政策效果或论文创新已经得到证明。
