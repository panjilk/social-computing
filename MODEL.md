# 模型说明（研究原型，未现实校准）

本文描述保留的阈值 A/B 与旧静态渠道基线。当前以国内与外部客户选择为重点的新模型见 [CHANNEL_MODEL.md](CHANNEL_MODEL.md)，独立入口 `experiments.channel_experiments`。新模型不使用这里的投诉阈值作为行动前置条件，旧基线及其输出不覆盖。

## 参考边界

Granovetter (1978), *Threshold Models of Collective Behavior*, American Journal of Sociology 83(6), 1420–1443, DOI: https://doi.org/10.1086/226707 。2026-09-13 可访问出版页摘要，未取得全文；这里只依据摘要确认阈值与群体行动概念，不声称复现其全文推导或原始实验。

NDlib 网络实现文档：https://ndlib.readthedocs.io/en/latest/reference/models/epidemics/Threshold.html 。它是后来的实现，非 1978 年作者代码。文档文字使用 greater than；本项目自行定义 >=，未验证 NDlib 源码边界行为。邻居比例阈值与二元扩散是基础借鉴；下面的案件、企业、时序、信号窗口及渠道全部是探索性建模假设。新闻没有进入参数或事实依据。

## A：阈值基线

无向无权图，节点编号 0 至 N−1。x_i(t) 为激活状态，阈值 θ_i∈[0,1]。

q_i(t)=Σ邻居 x_j(t)/degree(i)，孤立节点 q_i=0。

x_i(t+1)=x_i(t) OR [q_i(t)≥θ_i]。

严格同步，初始 seeds 无条件激活，激活不可逆。零阈值节点（含孤立节点）下一轮激活。达到固定点或最大轮数停止，历史包含初始状态与最后一次确认固定点的重复状态。该模块不含争议和企业。

## B：员工、企业与案件

员工固定属于一家企业，按 i mod F 建立近似均衡归属，再随机打乱。归属与网络独立，因此有跨企业边；没有声称这符合真实职场聚类。默认 ER 图，边概率 k/(N−1)；稳健性比较 WS（偶数 k，重连概率 .15）和 BA（m=k/2）。平均度仅大致可比，度分布和聚类也同时改变，不能把网络比较解读为单一拓扑统计量的因果效应。

阈值 θ_i=clip(θ+Uniform(−spread,spread),0,1)。初始争议与发起者分别进行 Bernoulli 抽样，发起者是固定个体特征，在以后新案件中仍可绕过阈值。每人最多一个未解决案件；案件号是 employee:created_round。同一人结案后的新争议是新案件。

案件状态：unsubmitted（存在争议尚未投诉）→pending（已收到，可能已回应）→resolved。已回应时间独立记录；回应不是解决。失败案件仍为 pending，没有自动撤案、期限结案、员工离职或无人投诉案件的自发解决。员工没有争议时不能投诉，也不重复提交同一案件。

员工观察“仍在进行或近期发生过投诉行动”的邻居比例 q_i。默认 observation_mode=pending_or_recent、visibility_window=W=3。设 s_j 为员工 j 最近一次提交轮次，P_j(t−1) 表示上一轮末有待处理投诉，则行动可见指标 a_j(t)=P_j(t−1) OR [s_j≥0 且 1≤t−s_j≤W]，q_i(t)=Σ邻居 a_j(t)/degree(i)。孤立节点比例为 0。

提交在 s 轮发生，就在 s+1 至 s+W 轮可见；即使当轮解决也不删除这段行动信息。超过窗口但仍在处理的投诉继续可见；已解决且超过窗口才退出信号。同一员工有多次历史案件、或同时符合两个条件，都只计一个邻居。W 是演示参数，不是现实测量值。员工不知道全公司投诉总数，且没有加入成功经验或成功学习：这里只记得“对方投诉过”。

条件仍是有 unsubmitted 案件且（固定发起者 OR q_i≥θ_i）。每轮开始冻结整份行动可见向量，再同步决策，当轮新提交不会在同轮传播。observation_mode=pending_only 仅保留作旧规则对照，可复现原来的“结案即移除信号”设定，不是当前默认。

每轮顺序：

1. t=0 注入初始争议，计入该轮 new_disputes；t>0 对没有未解决案件者抽取新争议。
2. 根据上一轮末待处理状态和截至上一轮的提交记录计算可见行动，统一确定并登记提交（received），更新最近提交时间。t=0 的信号全零。
3. 若响应开启，submitted+delay≤t 的案件记录首次 responded。回应不占处理容量，是简化的统一确认事件。
4. 每企业最多处理 capacity 个到期案件；按尝试次数少者优先，再按提交时间、员工号排序。每案每轮最多一次；失败下轮继续公平轮转。每次尝试以 effectiveness 概率解决。
5. 结案、一次性记单位补偿、更新未解决集合，记录轮末 pending 信息及指标；不删除最近提交时间。

delay 是收到至具备回应/处理资格的最少轮数；capacity 是每企业每轮处理尝试的案件上限；effectiveness 是每次尝试的成功概率。三者独立。无响应关闭回应与尝试。零容量允许回应但无法解决。delay=0 允许当轮收到、回应、解决，处理时间可为 0。因失败可重试，effectiveness 不是最终结案比例；在无限时间、正容量和正成功率等条件下，不能把其值解释成最终成功率。

新争议率 λ 只作用于当轮开始没有未解决案件的人，t=0 不重复抽取。可选预防机制：企业曾成功结案后，其 λ 乘以 (1−prevention)，从下一轮起生效，永久保持；这是故意简单的探索假设，不是企业学习理论。默认 λ=0、prevention=0。响应使员工重新有资格发生新案件，因此即使 λ 一样，新增案件总数也可能不同；这是风险集差异，并非随机配对失败。

轮次是抽象单位，无真实天数含义。B 固定运行 steps 轮，不因一轮没有新投诉提前结束（仍可能有排队、重试、新争议）。有限观察期未解决案件是右删失，不能给其处理时间填 0。

## C：可关闭的渠道扩展

channels_enabled=false 时不进入渠道模块。打开后，只有先通过 B 的争议与阈值门槛者选择 S∈{空集、domestic、external_customer、二者}。domestic 是聚合的国内处理通道，未拆分企业内部、人社、仲裁等机构；external_customer 仅指外部商业客户，不包含外国监管机构或港交所。这里不模拟机构自身的程序与管辖权。

主观概率 p̂_id 与 p̂_ie 是各自配置中心加独立 Uniform(−belief_spread,belief_spread) 后截断至 [0,1]，每员工固定。它们不随真实成功结果学习。

U(空集)=0；U({d})=p̂_id B−c_d；U({e})=p̂_ie B−c_e；

U({d,e})=[1−(1−p̂_id)(1−p̂_ie)]B−c_d−c_e−c_extra。

取效用最大者，相等时按空集、国内、客户、双渠道顺序选择。双渠道的主观公式假设两个成功机会独立；收益 B 只计算一次。效用为负时不投诉，案件仍为 unsubmitted。无动态更新或换渠道，所以静态效用不足者可能一直不提交。

企业客观每次处理概率：没有客户渠道时为 p；包含客户时 min(1,p(1+d·b))，d=customer_dependence，b=external_effect_boost。这是可关闭（b=0）的正向压力假设，**不是得到的研究发现**，也不意味着外部渠道使用必然增加。d 是人为定义的客户压力敏感度，不等于境外收入占比。所有企业默认同值，没有现实估计。

多渠道共享 case_id、同一企业队列与一次成功抽样，占用一个容量名额，最多结案一次、支付一次单位补偿。B 是主观收益单位；输出 compensation=1 是记账单位，非现实货币金额。

## 指标与实验识别

trajectory 中 new_* 是本轮流量；pending、unresolved、unsubmitted 是轮末存量。visible_complainants 是本轮决策时全网络可见行动者的去重人数，供研究者诊断，并非每位员工实际看到的总数，也不是轮末 pending。累计投诉人数按员工去重；total_submitted 按案件去重，新案件可使同一个员工再次被计为本轮投诉者。始终满足 total_cases=unresolved+cumulative_resolved，unresolved=pending+unsubmitted。

cases.processing_time=resolved−submitted，只对已解决案件定义；dispute_duration 从争议产生算起；observed_pending_time 对未解决已提交案件记录观察到末轮的等待时长。endpoints 同时提供 unresolved_fraction（分母所有产生案件）与 submitted_unresolved_fraction（分母已提交案件）。没有分母或没有已解决案件时记 NaN，而非 0。各运行的成功案件平均处理时间再跨运行汇总，各运行等权，count 列报告有效运行数，不是把全部成功案件混合加权。

domestic_case_share、external_case_share、multi_case_share 的分母都是该运行累计提交的独立案件数，双渠道同时进入前两项，前两项之和可超过 1；不是人口占比。trajectory 的渠道指标是每轮使用该渠道的新案件数。未投诉选项不进入已提交案件分母，可从 utility_abstained 观察每轮因静态效用放弃的人数（不是累计独立人数）。

SeedSequence(seed).spawn(4) 分离网络、初始条件、个体差异和事件；渠道使用 [seed,4,employee] 的独立键。所有情景使用连续 seeds=0..19，不筛选。事件随机数预生成为 [轮,员工] 数组，分别用于到达和解决，不依赖分支消耗次数。响应方案共用网络、归属、阈值、初始状态及外生抽样；结果分歧后不保证案件可逐一匹配，这是共同随机数法而非完全相同案件队列。不同拓扑用同一网络种子，但边集本就不同。

批量设计只在 immediate 基准上逐项变延迟、效果、阈值。新争议在 λ=.025 的基础上单独加入 prevention=.5。WS 和 BA 各包含三个响应方案。C 是附加演示，不混入 A/B 主比较。另增加 W=1、W=6（均相对默认 W=3）和三个 legacy_* 旧观察模式对照，共 26 个情景（含 C）、520 次运行。图带/误差棒是独立重复运行间均值±1 样本标准差，不是置信区间；paired_differences 报告先按 seed 做差再汇总的均值和差值 SD。

## 扩散停止的诊断和限制

不能仅凭曲线变平判断原因。threshold_blocked=eligible_unsubmitted 表示本轮剩余候选者都受阈值阻挡，但一轮阻挡不能证明未来永不传播，因为当轮可能仍有新提交。须结合 new_complaints、pending、visible_complainants、新争议开关和窗口衰减。全部案件解决是风险集耗尽；pending>0 且无人新提交可能只是可行动者耗尽或阈值阻挡，队列仍在运作。effect_0 对照可识别没有解决时是否仍然停滞；threshold_low/high 观察阈值敏感性；new_on 对照有限初始案件。有限轮数不能证明永远停止。

旧版本把当轮结案行为从下一轮信号中移除，已修正。当前版本保留已发生行动的有限窗口，但不是已经验证的现实传播机制：假定信息一轮后传达、同一邻居间完全可见、等权且共同窗口长度，仍需数据检验。成功经验是独立的候选机制，当前未混入行动信号。未投诉争议仍不能自行解决，普遍整改仍未建模，所以不能据投诉数量判断企业整体处理好坏。

可校准候选参数包括争议风险、阈值分布、网络结构及企业内外边比例、响应时间分布、容量、分次处理成功率、投诉可见时间、渠道主观概率和成本、预防效果、客户依赖的操作化量表。需要适当的数据与文献；不能直接从投诉数量推断政治信任或政治权威，也不保证论文创新或发表。
