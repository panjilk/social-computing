# 企业—商业客户网络扩展（探索版）

本模块研究固定国内正式渠道与商业客户渠道下，企业连接集中程度如何通过客户审核队列影响渠道竞争。不启用制度改善、公告、材料帮助、新争议或预防。旧主模型未启用扩展时规则不变。

## 主体与两种网络

保留员工信息网络；新增企业—商业客户二部网络。第一版每家企业恰有一个主要客户，多家企业可以共享客户。客户不是外国监管机构或多方治理组织。企业客户依赖沿用原模型变量，不等同海外收入比例。边上的 dependence 是企业对其主要客户的依赖指标，不是已估计的订单份额。

默认6企业、6客户，分别连接6、3、1个客户；按独立随机流 SeedSequence([seed,701]) 打乱企业后均衡分配。客户总数始终6，每个客户每轮审核2案，总名义容量12。分散为每客户1家企业，中间为3客户各2家企业，集中为1客户6家企业。其余客户依然存在，但没有本研究企业的连接，容量不能借用。

实验将客户存在概率和员工可达概率固定为1，以隔离不可达性；这是相对历史主实验的明确覆盖，各网络对照内一致。其他员工特征、初始案件、社交网络、企业依赖与事件随机流配对固定。所有值未经现实校准。

## 执行规则

仅替换客户审核调度：原模型的全局FIFO审核队列改为按案件所属企业的客户分组，在各组内依提交时间、员工编号排序，每轮最多审核2案。审核概率、客户介入后企业落实概率均不变。其余轮内执行顺序沿用 src/channel_choice.py。

用 A_fc 表示连接，Q_c(t) 为满足最短等待且未结案的客户审核队列，审核数为 min(|Q_c(t)|, μ_c)。总名义容量为 Σμ_c=12，不随集中度变动。可审核积压为审核选择后 |Q_c|-审核数；在当轮国内落实发生前测量，不等于轮末全部待办数。

容量共享诊断取消每客户上限，统一从全局FIFO取12案。它不是现实合作承诺，而是排除局部资源分割的计算对照。共享时不同连接图应产生完全相同的轨迹，程序逐种子断言。

员工尚不观察客户连接与排队长度，初始预计等待固定。网络影响只能经实际等待、结果观察和后续追加传递。该版本不用于证明首次选择的拓扑效应，也不允许员工在多个商业客户间选择。

## 实验、指标与边界

3集中度×2容量规则×3渠道组合×20种子=360次，先进行2种子的36次演示。每次验证案件守恒、唯一结案、唯一补偿；配对员工一致；仅国内组和容量共享组的拓扑不变性。

导出逐轮、案件、客户边、队列、端点、配对区间、种子0事件和决策。客户使用/首次/追加/解决比例均以全部初始争议案件为分母；渠道使用允许重叠。已审核等待均值不包含未审核者；同时报告未审核已提交数，该数包括取消案件，不可当成失败率。图为均值±1样本标准差；区间文件是按独立种子bootstrap的95%逐项区间。

固定名义总容量不等于固定可利用容量。集中效应包含局部容量闲置与瓶颈，不能称作纯拓扑因果效应。尚未做按客户连接数量分配容量的对照、一般多客户连接、全局敏感性或现实校准。事件随机数按轮次和员工索引，改变等待会改变实际使用的抽签；同一轮的潜在抽签在情景间一致。

## 运行

```powershell
.\run.ps1 -m unittest discover -s tests -q
.\run.ps1 -m experiments.customer_network --repeats 2 --out outputs/customer_network_small
.\run.ps1 -m experiments.customer_network
.\run.ps1 -m experiments.customer_network_contrasts
```

配置 configs/customer_network.json；新结果 outputs/customer_network_v1。原 competition 系列输出保留，不与本实验数值混用。不能将本扩展直接写成已确认创新；需要进一步核查供应链网络、排队与申诉渠道选择文献及客户程序依据。

最后一条命令只分析已有端点，生成 topology_contrasts.csv 与 TOPOLOGY_CONTRASTS.md，报告集中减分散的配对差和区间。现有 paper/MANUSCRIPT_ZH.md 尚未整合本扩展，不能把旧初稿视为新增研究的最终稿。
