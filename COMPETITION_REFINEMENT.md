# 制度竞争：对称诊断、等待预期与竞争条件

2026-09-18。当前方向仍是国内正式渠道与外部商业客户渠道的制度竞争。
全部参数是探索设定，未进行真实行为校准。本轮不增加政策改善、机构博弈或个体法律建议。

## 如何运行

```powershell
.\run.ps1 -m unittest discover -s tests -q
.\run.ps1 -m experiments.competition_refinement --out outputs/competition_refinement_small --repeats 2
.\run.ps1 -m experiments.competition_refinement
.\run.ps1 -m experiments.refinement_readout
.\run.ps1 -m experiments.symmetry_mirror
```

参数：`configs/competition_refinement.json`，继承动态批次配置。
正式结果单独保存在 `outputs/competition_refinement_v1/`。旧输出不覆盖。

## 1. 平局与对称诊断

旧配置默认 `tie_break=legacy`，保持等待、国内、客户、同时的候选顺序。
新实验 `tie_break=random`：仅在效用严格相等时，对所有最大效用候选均匀抽签，包括等待和同时提交。
随机流 `[seed,100,31]` 按轮次与员工索引，与网络、特征、事件流独立。
不把非常接近但不相等的效用人为当成平局。

对称诊断匹配两侧可达性、先验、基础成本、延迟和每个个体的客观阶段概率。
国内第一阶段 `0.6*(0.5+0.5*merit)` 与客户第一阶段 `0.3+0.3*merit` 相等；
两侧第二阶段均0.85。容量设为人口规模，排除全局/企业队列结构差异。
关闭社会成本效应和结果学习，独立同分布成本保留。比较跨种子分布，不要求单次两侧恰好相等。
单独的精确平局单元测试强制两侧个体成本、概率相等，确保随机规则不会固定偏向国内。

对称诊断不代表现实制度等同；它用于检查标签/实现偏向。连续个体成本下精确平局通常少见，
因此纠正平局规则不保证改变总体曲线。

正式20种子对称情景出现约5个百分点的客户优势，未将其隐藏。额外镜像诊断对每个种子交换
两侧个体成本、先验以及两阶段事件随机流，检查逐案两侧提交/结束时刻互换、解决时刻不变。
原世界与镜像世界合并用于实现对称性诊断，不替代原20个种子的探索结果，也不证明真实制度等效。

## 2. 可关闭的等待预期学习

默认 `wait_learning_weight=0`，保持旧行为；实验取0.5，先验强度2。
在本轮决策之前，员工仅使用自己的案件以及固定分享关系下邻居的可见案件。
已成功或已结束未解决的渠道，若完成时间在最近W轮内，观察值为完成时间减提交时间。
仍在处理的渠道，不论提交多早，观察值为当前轮减提交轮（右删失下界）。
因另一渠道成功而取消的渠道排除；本轮新提交和本轮结果均不能提前观察。

对员工i、渠道k，设固定初始等待判断w0、先验强度a、可见观察值x：

`w_hat(i,k,t) = (1-lambda)*w0 + lambda*(a*w0 + sum(x))/(a+n)`。

每轮从固定先验和当前可见集合重新计算，不重复累积同一条观察。
学习后的等待用于原效用中的等待折扣及已提交案件的超时判断。
完成失败的时长是渠道给出终局结果的等待，不是补救成功的等待。

这是可解释的主观学习启发式，**不是无偏的客观平均处理时长估计**。
未完成年龄只是下界，可能低估最终等待；没有把取消视为正常办结，也没有采用全局真实队列。
在没有数据的情况下，不声称lambda=0.5比固定预期更符合现实。

## 3. 完整预定实验

共27情景，连续种子0—19，不按结果筛选：

- 旧基准、随机平局基准、等待学习、等待学习但关闭结果学习：4组。
- 观察期12/48轮；批次间隔3/12轮；结果记忆6/36轮：6组，分别只改变一项。
- 对称诊断随机/旧平局：2组。
- 企业均有客户，员工可达概率0.4/0.7/1.0 × 客户与国内基础成本比0.5/1/1.5：9组。
- 客户后续落实概率0.2/0.5/0.8，关闭其依赖加成：3组；主观初始判断保持不变。
- 客户初始预期0.25/0.5/0.75：3组；客观处理机制保持不变。

上述总计27组。网格中仅员工可达概率改变，企业有客户统一设为1；不可把与原基准的差
全归于个人可达性。成本是基础成本比，个人实际成本仍乘独立异质性因子。
后续落实概率不是整个客户渠道成功率，前面仍需客户审核介入。
初始预期经既有异质性及裁剪形成个人概率，不保证实际群体均值恰好等于参数。

每个情景内每批观察相同H，模拟运行长度自动调整。相同种子保留员工身份、企业归属、
争议资格、批次分配和个体基础随机数；可达、时点、决策和结果可以因干预而改变。
延迟导致事件使用不同轮次的随机数，这是共同随机数设计的限制。

## 4. 统计与图表

分母为对应批次的实际争议员工，另报两渠道均可达人群。
后期为最后两批、前期为最初两批；先按种子内案件数合并，再跨种子等权平均。

- `robustness.png`：左侧后期客户减国内，右侧客户早晚变化，逐项bootstrap95%区间。
- `competition_map.png`：客户可达性×相对成本；颜色为客户减国内均值，文字按逐项区间判定。
- `exclusive_paths.png`：仅国内、仅客户、双渠道并用、未投诉，四者合计100%，展示均值，不附区间。
- `belief_vs_effect.png`：分别改变客观后续落实概率和主观初始预期，使用比例与逐项95%区间。
- `intervals.csv`：水平、早晚变化、相对基准的配对差及区间；不是经多重比较校正的确认性检验。
- `cohorts.csv`：逐批、逐种子的首次选择、追加、使用、解决及互斥路径。
- `decision_waits.csv`：当轮被评估决策的案件平均等待判断；不是全体员工平均。
- `demo/`：种子0各情景案件；部分情景的逐次决策。
- `verification.json`：完整情景配置、种子和源码哈希。
- `symmetry_mirror.csv/json`：20种子原世界+镜像世界共40次额外诊断，不计入27情景主实验。
- `FINDINGS.md`、`additional_checks.json`：运行readout后生成的通俗解释与旧基准回归、分母一致性核查。

客户使用增长、客户超过国内、双渠道追加是不同问题。客户增长可能来自追加，
不能只看使用总量就称替代。固定短批次模拟不能证明长期稳态或现实未来年份的变化。
